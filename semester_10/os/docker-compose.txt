version: '3.6'
services:
  db:
    container_name: areas-postgres
    image: postgres:14-alpine
    restart: always
    ports:
      - "5431:5432"
    volumes:
      - db:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: areas
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
volumes:
  db:
    driver: local
